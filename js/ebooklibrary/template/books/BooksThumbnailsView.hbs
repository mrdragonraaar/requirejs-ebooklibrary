{{!
{
	"name": "ThumbsView",
	"description": "Books Panel Thumbs View Template"
}
}}
<div class="bookspanel-books bookspanel-thumbs row multi-columns-row">
{{#each books}}
	<div class="book-thumb col-lg-2 col-md-3 col-sm-4 !col-ms-4 col-xs-6">
		<div class="thumbnail book-thumbnail">
			<a href="{{downloadUrl}}" title="{{fileInfo.name}}"><img src="{{bookInfo.cover}}"/></a>
			<h3 class="book-author" title="{{bookInfo.authorStr}}">{{bookInfo.authorStr}}</h3>
			<h4 class="book-title" title="{{bookInfo.title}}">{{bookInfo.title}}</h3>
		</div>
<!--
		<div class="book-thumb-cover">
			<div>
				<a href="{{downloadUrl}}" title="{{fileInfo.name}}"><img src="{{bookInfo.cover}}"/></a>
			</div>
		</div>
		<h5 class="heading-author" title="{{bookInfo.authorStr}}">{{bookInfo.authorStr}}</h5>
		<h4 class="heading-title" title="{{bookInfo.title}}">{{bookInfo.title}}</h4>
-->
	</div>
{{/each}}
</div>
